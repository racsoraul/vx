- hosts: localhost
  become: true
  pre_tasks:
    - name: Loading environment variables
      tags: always
      set_fact:
        vx_root: "{{ lookup('env', 'VX_ROOT') }}"
  tasks:
  - name: Install tools
    apt:
      name: ['htop', 'tmux']
  roles:
    - vx.install-blender
